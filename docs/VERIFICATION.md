# 本轮生成与平台连接验证

2026-09-12：46 项自动测试、类型检查与生产构建通过。新增 GPT Image 2 → Seedance、抖音 OAuth/定时投稿/审核查询、评论分页同步，均以模拟响应测试；没有调用真实付费接口或发布作品。设置页完成桌面和手机布局检查，安卓/iOS 工程已同步网页资源。完整说明见 [生成与平台连接](GENERATION-AND-PLATFORMS.md)。

# v0.2 API 与本机密钥验证

2026-09-12：34 项自动测试、类型检查与生产构建通过。增加真实 Windows DPAPI 加密重开和篡改拒绝测试；浏览器验证虚构密钥保存、刷新、删除与模型分工修改，未调用付费 API。详见 [v0.2 完整记录](API-AND-MODELS.md)。

以下是保留的 v0.1 验证记录，旧 GPT-5 描述仅指当时实现。

# 本轮验证记录

日期：2026-09-12。环境：Windows、Node.js 24.19、Codex 内置 Chromium 浏览器。

## 自动检查

- TypeScript 严格检查通过。
- Vite 生产构建通过，生成 `dist/`；入口、PWA 图标、manifest 和 service worker 均存在。
- **26 / 26** 业务和接口测试通过（`node --test tests/*.test.mjs`）。
- 核心覆盖：确认幂等、锁定策略与资料快照、局部重做保留其他素材、并发修改拦截、文件格式签名、发布快照与防重复、对应平台链接校验、评论去重/批评保留、证据范围、反馈采用和撤销、事务回滚。
- API 覆盖：参数验证、跨站来源限制、密码登录与注销、密钥不泄漏、调用前预算拦截、模型拒绝/不完整返回/网络不确定状态、新建项目、并发生成与旧结果丢弃、真实 ZIP 响应。
- GPT-5 请求通过模拟 HTTP 响应检查 `model`、严格 JSON schema、`store:false` 与费用记录；**没有使用真实 API 密钥实测**。

## 浏览器操作

1. 打开示例项目，查看可编辑策略，确认 V1；对应图文和视频任务创建成功。
2. 实际生成 PNG 封面与三段式 MP4，均保存到数据库。
3. 验证视频元数据：**540 × 960，11.9941 秒，readyState 4**。此处没有上传配音，是无声短片。
4. 查看生成的 900 × 1200 PNG，标题、品牌和插画正常显示。
5. 导入 10 条明确标注的示例评论；生成 6 组本地规则复盘；采用「使用疑问」建议。
6. 生成下一轮 V2 草稿，创作方向与 Prompt 中显示已采用的规格/实物比例/使用演示要求，V1 与现有成品保留。
7. 创建小红书图文发布计划，生成保存版本的素材 ZIP。服务端返回文件已下载到验证目录；浏览器 **download 事件已确认**。
8. 未向任何社交平台投稿；登记结果的 URL/确认约束在隔离 API 测试中验证。
9. 刷新页面并重启服务后，策略、成品、评论、采用的经验与发布计划保留。
10. 390 × 844 手机视口检查策划页、策略页和生成对话框；未出现横向溢出。手机导航在关闭时隐藏，避免键盘焦点进入屏外侧栏。

验证样例保存在忽略目录 `test-results/`，包括 `sample-image.png`、`sample-video.mp4`、`sample-package.zip`，仅为示例内容。

## 移动端与部署

- 安卓工程生成成功，Filesystem/Share 插件与最新网页资源同步成功。
- iOS 工程生成成功，使用 Swift Package Manager，文件分享插件同步成功；隐私清单已写入 Xcode 资源列表。
- 已生成网页和原生图标。
- 未安装 Android Studio/SDK，未编译 APK/AAB。
- 当前是 Windows，未编译、签名或真机验证 iOS。
- Docker/Compose 提供部署配置，但本机未运行容器构建。
- 尚未测试 PWA 的系统安装流程、移动端相册/配音上传、系统文件分享和真机视频编码。

## 本轮发现并修正

- 已确认策略编辑改为派生草稿，并保存原始品牌资料快照。
- 标题改动使画面重新进入待制作，未完成版本不冒充可恢复成品。
- 网络结果不明时保留费用预留；项目修改期间的旧模型结果不会覆盖新资料。
- 增加服务端 ZIP 下载与短期专用链接，避免只依赖浏览器 Blob 下载。
- 端口占用时启动程序明确报错，不再输出误导性的成功地址。
